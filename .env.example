# Neon Database (shared with dashboard)
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Anthropic API Key (for syncing models)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Authentication (shared with dashboard for admin access)
SESSION_SECRET=your_session_secret_here

# API Key for Projects (generate a strong random string)
# Used by ai, tracker, forms projects to access this service
MODELS_API_KEY=mod_...

# Cron Job Secret (for Vercel cron authentication)
CRON_SECRET=your_cron_secret_here

# Slack Webhook URL (for alerts)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Email Alerts (optional)
ALERT_EMAIL=cameron@brasshelm.com
